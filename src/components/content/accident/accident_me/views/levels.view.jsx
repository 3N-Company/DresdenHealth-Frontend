import React from 'react';
import PropTypes from 'prop-types';

const LevelsView = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

LevelsView.propTypes = {

};

export default LevelsView;
